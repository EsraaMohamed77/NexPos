<UserControl x:Class="NexPos.Views.ItemTypeView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
             mc:Ignorable="d" 
             d:DesignHeight="550" d:DesignWidth="900">

    <Grid Background="#F8F9FA">
        <Grid.RowDefinitions>
            <RowDefinition Height="70"/>
            <RowDefinition Height="*"/>
        </Grid.RowDefinitions>

        <!-- Header -->
        <Border Background="#2C3E50" Grid.Row="0">
            <DockPanel Margin="25,0">
                <TextBlock Text="أنواع الأصناف (Item Types)" 
                           VerticalAlignment="Center"
                           FontSize="24" 
                           FontWeight="SemiBold" 
                           Foreground="White"/>

                <Button Command="{Binding AddCommand}" 
                        DockPanel.Dock="Right"
                        Width="160" Height="45"
                        Background="#27AE60" 
                        BorderThickness="0"
                        Margin="0,0,10,0"
                        Content="إضافة نوع جديد"
                        FontSize="15"/>
            </DockPanel>
        </Border>

        <!-- DataGrid -->
        <DataGrid Grid.Row="1"
                  Margin="25"
                  ItemsSource="{Binding ItemTypes}"
                  SelectedItem="{Binding SelectedItemType}"
                  AutoGenerateColumns="False"
                  GridLinesVisibility="None"
                  RowHeight="55"
                  FontSize="15">

            <DataGrid.Columns>
                <DataGridTextColumn Header="الاسم" Binding="{Binding Name}" Width="*" MinWidth="250"/>
                <DataGridTextColumn Header="لون الخلفية" Binding="{Binding BgColor}" Width="180"/>
                <DataGridCheckBoxColumn Header="يستخدم صورة" Binding="{Binding UseBgImage}" Width="140"/>

                <DataGridTemplateColumn Header="إجراءات" Width="220">
                    <DataGridTemplateColumn.CellTemplate>
                        <DataTemplate>
                            <StackPanel Orientation="Horizontal" HorizontalAlignment="Center">
                                <Button Content="تعديل" 
                                        Width="85" Height="35"
                                        Command="{Binding DataContext.EditCommand, RelativeSource={RelativeSource AncestorType=DataGrid}}"
                                        CommandParameter="{Binding}"
                                        Background="#3498DB" Foreground="White"/>

                                <Button Content="حذف" 
                                        Width="85" Height="35" Margin="10,0,0,0"
                                        Command="{Binding DataContext.DeleteCommand, RelativeSource={RelativeSource AncestorType=DataGrid}}"
                                        CommandParameter="{Binding}"
                                        Background="#E74C3C" Foreground="White"/>
                            </StackPanel>
                        </DataTemplate>
                    </DataGridTemplateColumn.CellTemplate>
                </DataGridTemplateColumn>
            </DataGrid.Columns>
        </DataGrid>
    </Grid>
</UserControl>